namespace douUI {
    /**
     * Tile 布局的排列方向枚举
     * @author wizardc
     */
    export const enum TileOrientation {
        /**
         * 逐行排列元素
         */
        rows,
        /**
         * 逐列排列元素
         */
        columns
    }
}
